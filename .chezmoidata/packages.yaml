packages:
  arch:
    pacman:
      - btop
      - git
      - hyprshutdown
      - kitty
      - starship
      - jq 
      - zsh
    aur:
      - caelestia-shell-git
      - eza
      - gnome-keyring
      - hyprland
      - proton-pass-cli
      - ttf-firacode-nerd
      - uv
      - yazi
      - zed
  debian:
    apt:
      - uv
      - yazi
      - btop
# #!/bin/bash
# set -eufo pipefail
# sudo apt-get update
# sudo apt-get install -y \
# {{ range .packages.debian.apt -}}
#   {{ . | quote }} \
# {{ end }}
